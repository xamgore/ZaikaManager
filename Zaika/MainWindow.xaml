<controls:MetroWindow x:Name="TableWindow" x:Class="Zaika.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      Title="ZaikaDB Manager" Height="550" Width="555"
                      TextElement.Foreground="{DynamicResource MaterialDesignBody}"
                      Background="{DynamicResource MaterialDesignPaper}"
                      FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
                      WindowStartupLocation="CenterScreen" MinWidth="555"
                      BorderThickness="1" BorderBrush="{DynamicResource AccentColorBrush3}">
    <Grid>
        <ListBox x:Name="Operations" HorizontalContentAlignment="Stretch" Margin="0"
            ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Auto" />

        <Button Style="{StaticResource MaterialDesignFloatingActionAccentButton}"
            Margin="30" HorizontalAlignment="Right" VerticalAlignment="Bottom"
            Content="+" FontSize="30" Width="40" Height="40"/>
    </Grid>
</controls:MetroWindow>